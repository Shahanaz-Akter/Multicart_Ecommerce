const express = require('express');
const bodyParser = require('body-parser');
const models = require('../models');

const addCustomer = async (req, res) => {
    console.log('customer/add_customer.ejs');
}


module.exports = { addCustomer, }